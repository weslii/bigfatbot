<%- include('header', { title: 'Group Setup' }) %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h4 class="mb-0">Setup Your WhatsApp Groups</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success" role="alert">
                        <h5 class="alert-heading">Business Created Successfully!</h5>
                        <p>Your business "<%= businessName %>" has been registered. Now let's set up your WhatsApp groups.</p>
                    </div>

                    <div class="mb-4">
                        <h5>Setup Instructions</h5>
                        <ol>
                            <li class="mb-3">
                                <strong>Add the Bot to Your Groups</strong>
                                <p class="mb-0">Add our WhatsApp bot to your existing groups:</p>
                                <ul>
                                    <li>Sales group (where customers place orders)</li>
                                    <li>Delivery group (where delivery personnel manage orders)</li>
                                </ul>
                            </li>
                            
                            <li class="mb-3">
                                <strong>Use the Setup Command</strong>
                                <p>In each group, type the following command:</p>
                                <div class="alert alert-light border">
                                    <code class="user-select-all"><%= setupCommand %></code>
                                    <button class="btn btn-sm btn-outline-primary float-end" onclick="copyCommand()">Copy</button>
                                </div>
                            </li>

                            <li>
                                <strong>Verify Setup</strong>
                                <p class="mb-0">The bot will automatically detect the group type and configure it accordingly. You'll receive a confirmation message in each group.</p>
                            </li>
                        </ol>
                    </div>

                    <div class="alert alert-info" role="alert">
                        <h5 class="alert-heading">Important Notes</h5>
                        <ul class="mb-0">
                            <li>Make sure to add the bot to both groups</li>
                            <li>The bot will automatically determine which group is for sales and which is for delivery</li>
                            <li>You can always add more groups later through the dashboard</li>
                        </ul>
                    </div>

                    <div class="text-center mt-4">
                        <a href="/dashboard?userId=<%= userId %>" class="btn btn-primary">Go to Dashboard</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
function copyCommand() {
    const command = document.querySelector('code').textContent;
    navigator.clipboard.writeText(command).then(() => {
        const button = document.querySelector('button');
        button.textContent = 'Copied!';
        setTimeout(() => {
            button.textContent = 'Copy';
        }, 2000);
    });
}
</script>

<%- include('footer') %> 