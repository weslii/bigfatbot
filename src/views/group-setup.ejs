<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Group Setup - Novi</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="icon" href="/logo-icon copy.svg">
    <link href="/css/theme.css" rel="stylesheet">
    <style>
        :root {
            --primary: #10b981;
            --secondary: #3b82f6;
            --accent: #8b5cf6;
            --background: #f8fafc;
            --foreground: #1e293b;
            --muted: #64748b;
            --border: #e2e8f0;
        }
        [data-theme="dark"] {
            --background: #020617;
            --foreground: #f8fafc;
            --muted: #94a3b8;
            --border: #1e293b;
        }
        html {
            min-height: 100vh;
            height: auto;
            width: 100vw;
            margin: 0;
            padding: 0;
        }
        body {
            min-height: 100vh;
            height: auto;
            width: 100vw;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #f8fafc 0%, #e0f2fe 100%);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: var(--foreground);
            line-height: 1.6;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        [data-theme="dark"] body {
            background: linear-gradient(135deg, #020617 0%, #0f172a 50%, #064e3b 100%) !important;
        }
        .header {
            position: sticky;
            top: 0;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border);
            transition: all 0.3s ease;
        }
        [data-theme="dark"] .header {
            background: rgba(15, 23, 42, 0.8);
        }
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        .logo-icon {
            width: 2.5rem;
            height: 2.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 0.75rem;
            background: linear-gradient(135deg, #10b981, #14b8a6);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
        }
        .logo-icon i {
            font-size: 1.25rem;
            color: white;
        }
        .logo-text {
            font-weight: 700;
            font-size: 1.125rem;
            background: linear-gradient(135deg, #10b981, #14b8a6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .nav {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        .nav-link {
            color: var(--muted);
            text-decoration: none;
            font-size: 0.875rem;
            font-weight: 500;
            transition: color 0.3s ease;
            padding: 0.5rem 0.75rem;
            border-radius: 0.5rem;
        }
        .nav-link:hover {
            color: var(--foreground);
            background: var(--border);
        }
        .theme-toggle {
            background: transparent;
            border: none;
            color: var(--muted);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            transition: all 0.3s ease;
        }
        .theme-toggle:hover {
            color: var(--foreground);
            background: var(--border);
        }
        .setup-section {
            min-height: 100vh;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding: 2.5rem 0;
        }
        .setup-cards-wrapper {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .setup-heading {
            text-align: center;
            margin-bottom: 1.2rem;
        }
        .setup-almost {
            font-size: 1.7rem;
            font-weight: 900;
            letter-spacing: -0.5px;
            color: #10b981;
            margin-bottom: 0.1rem;
        }
        .setup-sub {
            font-size: 1.01rem;
            color: #64748b;
            font-weight: 500;
        }
        [data-theme="dark"] .setup-sub {
            color: #cbd5e1;
        }
        .setup-cards {
            width: 100%;
            max-width: 600px;
            display: flex;
            flex-direction: column;
            gap: 1.3rem;
            align-items: center;
        }
        .success-banner {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.7rem;
            background: linear-gradient(90deg, #bbf7d0 0%, #6ee7b7 100%);
            color: #166534;
            border: none;
            border-radius: 1.1rem;
            font-size: 0.97rem;
            font-weight: 600;
            padding: 0.7rem 1.1rem;
            margin-bottom: 0.7rem;
            box-shadow: 0 2px 12px rgba(16,185,129,0.07);
            text-align: center;
            letter-spacing: 0.01em;
            max-width: 540px;
        }
        .success-banner i {
            font-size: 1.13rem;
        }
        [data-theme="dark"] .success-banner {
            background: linear-gradient(90deg, #064e3b 0%, #134e4a 100%);
            color: #bbf7d0;
        }
        .step-card, .info-card, .dashboard-card {
            width: 100%;
            max-width: 540px;
            margin: 0 auto;
        }
        .step-card {
            display: flex;
            align-items: flex-start;
            gap: 1.1rem;
            background: rgba(255,255,255,0.97);
            border-radius: 1.1rem;
            box-shadow: 0 4px 24px 0 rgba(16, 185, 129, 0.06), 0 1px 8px 0 rgba(0,0,0,0.07);
            padding: 1.05rem 1.1rem 1.05rem 1.1rem;
            border: 1px solid var(--border);
            position: relative;
            transition: box-shadow 0.2s;
        }
        [data-theme="dark"] .step-card {
            background: rgba(22, 28, 44, 0.98);
            color: #fff;
        }
        .step-badge {
            min-width: 2.1rem;
            min-height: 2.1rem;
            max-width: 2.1rem;
            max-height: 2.1rem;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.97rem;
            color: #fff;
            margin-top: 0.13rem;
            background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
            box-shadow: 0 2px 8px rgba(16, 185, 129, 0.10);
        }
        .step-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 0.22rem;
        }
        .step-title {
            font-size: 0.99rem;
            font-weight: 700;
            margin-bottom: 0.05rem;
            letter-spacing: -0.01em;
        }
        .step-desc {
            color: var(--muted);
            font-size: 0.91rem;
            margin-bottom: 0.05rem;
            font-weight: 400;
        }
        .step-list {
            margin: 0.05rem 0 0.05rem 1.1rem;
            padding: 0;
            font-size: 0.91rem;
        }
        .step-confirm {
            color: var(--primary);
            font-size: 0.89rem;
            margin-top: 0.05rem;
        }
        .step-note {
            color: var(--muted);
            font-size: 0.87rem;
            margin-top: 0.13rem;
        }
        .copy-card {
            display: flex;
            align-items: center;
            background: linear-gradient(90deg, #f1f5f9 60%, #e0f2fe 100%);
            border-radius: 0.7rem;
            border: 1px solid #e2e8f0;
            padding: 0.18rem 0.5rem 0.18rem 0.8rem;
            margin: 0.25rem 0 0.05rem 0;
            width: 100%;
            justify-content: space-between;
            box-shadow: 0 1px 4px rgba(59,130,246,0.04);
            overflow: hidden;
            box-sizing: border-box;
            max-width: 100%;
        }
        [data-theme="dark"] .copy-card {
            background: linear-gradient(90deg, #1e293b 60%, #164e63 100%);
            border-color: #334155;
        }
        .copy-value {
            min-width: 0;
            max-width: 70%;
            overflow-wrap: break-word;
            word-break: break-all;
        }
        [data-theme="dark"] .copy-value {
            color: #e0e7ef;
        }
        .copy-btn {
            flex-shrink: 0;
            max-width: 30%;
            background: #10b981;
            color: #fff;
            font-weight: 600;
            font-size: 0.89em;
            border-radius: 0.5em;
            border: none;
            padding: 0.22em 0.9em;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5em;
            transition: background 0.18s, color 0.18s, box-shadow 0.18s;
            box-shadow: 0 1px 4px rgba(16,185,129,0.08);
            white-space: nowrap;
        }
        .copy-btn:hover, .copy-btn:focus {
            background: #059669;
            color: #fff;
            box-shadow: 0 2px 8px rgba(16,185,129,0.13);
        }
        .info-card {
            display: flex;
            align-items: flex-start;
            gap: 1.2rem;
            background: #e0f2fe;
            border-radius: 1.1rem;
            box-shadow: 0 2px 12px 0 rgba(59,130,246,0.07), 0 1px 4px 0 rgba(0,0,0,0.08);
            padding: 1.05rem 1.1rem 1.05rem 1.1rem;
            border: 1px solid #bae6fd;
            position: relative;
            color: #0369a1;
        }
        [data-theme="dark"] .info-card {
            background: #164e63;
            color: #bae6fd;
            border-color: #0ea5e9;
        }
        .info-badge {
            min-width: 2rem;
            min-height: 2rem;
            max-width: 2rem;
            max-height: 2rem;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.01rem;
            color: #fff;
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            margin-top: 0.13rem;
        }
        .info-title {
            font-size: 0.97rem;
            font-weight: 700;
            margin-bottom: 0.05rem;
        }
        .info-list {
            margin: 0.18rem 0 0 0;
            padding-left: 1.1rem;
            font-size: 0.91rem;
        }
        .info-list li {
            margin-bottom: 0.13rem;
            text-indent: 0;
        }
        .dashboard-card {
            background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
            border-radius: 1.3rem;
            box-shadow: 0 2px 16px 0 rgba(16, 185, 129, 0.10), 0 1px 8px 0 rgba(59,130,246,0.08);
            padding: 1.7rem 1.1rem 1.7rem 1.1rem;
            text-align: center;
            color: #fff;
            margin-top: 0.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        [data-theme="dark"] .dashboard-card {
            background: linear-gradient(135deg, #134e4a 0%, #1e293b 100%);
            color: #fff;
        }
        .dashboard-title {
            font-size: 1.13rem;
            font-weight: 800;
            margin-bottom: 0.7rem;
            letter-spacing: -0.5px;
        }
        .dashboard-desc {
            font-size: 0.97rem;
            color: #e0e7ef;
            margin-bottom: 0.5rem;
        }
        .dashboard-btn {
            background: #fff;
            color: #10b981;
            font-weight: 700;
            font-size: 0.97rem;
            border-radius: 0.8rem;
            padding: 0.5rem 1.7rem;
            text-decoration: none;
            box-shadow: 0 1px 6px rgba(16,185,129,0.10);
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 0.7rem;
            transition: background 0.18s, color 0.18s, box-shadow 0.18s;
        }
        .dashboard-btn:hover {
            background: #10b981;
            color: #fff;
            box-shadow: 0 2px 12px rgba(16,185,129,0.18);
        }
        [data-theme="dark"] .dashboard-btn {
            background: #fff;
            color: #134e4a;
        }
        [data-theme="dark"] .dashboard-btn:hover {
            background: #134e4a;
            color: #fff;
        }
        @media (max-width: 700px) {
            .setup-section {
                padding: 1.2rem 0 1.2rem 0;
            }
            .setup-cards-wrapper {
                max-width: 98vw;
            }
            .setup-cards {
                max-width: 98vw;
            }
            .step-card, .info-card, .dashboard-card, .success-banner {
                max-width: 98vw;
            }
        }
        @media (max-width: 500px) {
            .setup-section {
                padding: 0.5rem 0 0.5rem 0;
            }
            .setup-cards-wrapper {
                max-width: 100vw;
            }
            .setup-cards {
                max-width: 100vw;
            }
            .step-card, .info-card, .dashboard-card, .success-banner {
                flex-direction: column;
                align-items: stretch;
                gap: 0.7rem;
                padding: 0.7rem 0.3rem 0.7rem 0.3rem;
                max-width: 100vw;
            }
            .step-badge, .info-badge {
                margin-bottom: 0.5rem;
                margin-top: 0;
            }
            .copy-card {
                padding: 0.18rem 0.2rem 0.18rem 0.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <div class="logo">
                <div class="logo-icon">
                    <!-- Removed WhatsApp icon -->
                </div>
                <div class="logo-text"><img class="theme-logo" src="/logo-animated.svg" alt="Novi Logo" style="height:56px;vertical-align:middle;"></div>
            </div>
            <nav class="nav">
                <a href="/" class="nav-link">Home</a>
                <a href="/login" class="nav-link">Business Login</a>
                <a href="/admin/dashboard" class="nav-link">Admin</a>
                <button class="theme-toggle" id="themeToggle">
                    <i class="fas fa-sun"></i>
                    <i class="fas fa-moon"></i>
                </button>
            </nav>
        </div>
    </header>
    <section class="setup-section">
        <div class="setup-cards-wrapper">
            <div class="setup-heading">
                <div class="setup-almost">Almost there!!</div>
                <div class="setup-sub">Now set up your WhatsApp groups</div>
            </div>
            <div class="setup-cards">
                <!-- Success Banner -->
                <div class="success-banner">
                    <i class="fas fa-check-circle"></i>
                    <span>Business created! <b><%= businessName %></b> is ready to go.</span>
                </div>
                <!-- Step 1 Card -->
                <div class="step-card">
                    <div class="step-badge">1</div>
                    <div class="step-content">
                        <div class="step-title">Add the Bot to Your Group</div>
                        <div class="step-desc">Add this WhatsApp number to your group as an admin:</div>
                        <div class="copy-card">
                            <span id="botNumber" class="copy-value">Loading...</span>
                            <button class="copy-btn" onclick="copyBotNumber()"><i class="far fa-copy"></i> Copy</button>
                        </div>
                        <small class="step-note">Give the bot admin permissions for full functionality.</small>
                    </div>
                </div>
                <!-- Step 2 Card -->
                <div class="step-card">
                    <div class="step-badge">2</div>
                    <div class="step-content">
                        <div class="step-title">Run the Setup Command</div>
                        <div class="step-desc">In your WhatsApp group, type this exact command:</div>
                        <div class="copy-card">
                            <span class="copy-value user-select-all">/setup <%= business.setup_identifier || business.business_id %></span>
                            <button class="copy-btn" onclick="copySetupCommand()"><i class="far fa-copy"></i> Copy</button>
                        </div>
                    </div>
                </div>
                <!-- Step 3 Card -->
                <div class="step-card">
                    <div class="step-badge">3</div>
                    <div class="step-content">
                        <div class="step-title">Reply with Group Type</div>
                        <div class="step-desc">The bot will ask if this is a <b>"sales"</b> or <b>"delivery"</b> group. Reply in the group with:</div>
                        <ul class="step-list">
                            <li><b>"sales"</b> – For receiving customer orders</li>
                            <li><b>"delivery"</b> – For managing deliveries and tracking orders</li>
                        </ul>
                        <div class="step-confirm">The bot will confirm when the group is registered.</div>
                    </div>
                </div>
                <!-- Step 4 Card -->
                <div class="step-card">
                    <div class="step-badge">4</div>
                    <div class="step-content">
                        <div class="step-title">Repeat for Second Group</div>
                        <div class="step-desc">To add your other group (sales or delivery), repeat these steps in your other WhatsApp group.</div>
                    </div>
                </div>
                <!-- Important Notes Card -->
                <div class="info-card">
                    <div class="info-badge"><i class="fas fa-info-circle"></i></div>
                    <div class="info-content">
                        <div class="info-title">Important Notes</div>
                        <ul class="info-list">
                            <li>Make sure to add the bot to both groups if you want to use both sales and delivery features.</li>
                            <li>The bot will automatically determine which group is for sales and which is for delivery.</li>
                            <li>You can always add more groups later through the dashboard.</li>
                        </ul>
                    </div>
                </div>
                <!-- Dashboard Card -->
                <div class="dashboard-card">
                    <div class="dashboard-title">Ready to Start Managing Orders?</div>
                    <div class="dashboard-desc">Once you've completed the setup, access your dashboard to manage orders and track deliveries.</div>
                    <a href="/dashboard?userId=<%= userId %>" class="dashboard-btn">Go to Dashboard <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </div>
    </section>
<script>
        
// Load bot number when page loads
document.addEventListener('DOMContentLoaded', async function() {
  try {
    const response = await fetch('/api/whatsapp/bot-info');
    const botInfo = await response.json();
    if (botInfo.success) {
      document.getElementById('botNumber').textContent = botInfo.number;
    } else {
      document.getElementById('botNumber').textContent = 'Not available';
    }
  } catch (error) {
    console.error('Error loading bot info:', error);
    document.getElementById('botNumber').textContent = 'Error loading';
  }
});
function copyBotNumber() {
    const botNumber = document.querySelector('#botNumber').textContent;
    if (botNumber === 'Loading...' || botNumber === 'Not available' || botNumber === 'Error loading') {
        alert('Bot number not available yet. Please try again in a moment.');
        return;
    }
    navigator.clipboard.writeText(botNumber).then(() => {
        const button = event.target;
        button.textContent = 'Copied!';
        setTimeout(() => {
            button.textContent = 'Copy';
        }, 2000);
    });
}
function copySetupCommand() {
            const command = document.querySelector('.user-select-all').textContent;
    navigator.clipboard.writeText(command).then(() => {
        const button = event.target;
        button.textContent = 'Copied!';
        setTimeout(() => {
            button.textContent = 'Copy';
        }, 2000);
    });
}
</script>
    <script src="/js/theme-utils.js"></script>
</body>
</html> 