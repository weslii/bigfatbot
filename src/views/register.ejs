<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register Your Business - Novi</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/theme.css" rel="stylesheet">
    <link rel="icon" href="/logo-icon copy.svg">
    <style>
        :root {
            --primary: #10b981;
            --secondary: #3b82f6;
            --accent: #8b5cf6;
            --background: #f8fafc;
            --foreground: #1e293b;
            --muted: #64748b;
            --border: #e2e8f0;
        }
        [data-theme="dark"] {
            --background: #020617;
            --foreground: #f8fafc;
            --muted: #94a3b8;
            --border: #1e293b;
        }
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--background);
            color: var(--foreground);
            line-height: 1.6;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        [data-theme="dark"] body {
            background: linear-gradient(135deg, #020617 0%, #0f172a 50%, #064e3b 100%);
        }
        .header {
            position: sticky;
            top: 0;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border);
            transition: all 0.3s ease;
        }
        [data-theme="dark"] .header {
            background: rgba(15, 23, 42, 0.8);
        }
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.5rem 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
        }
        .logo {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        .logo-icon {
            width: 2.5rem;
            height: 2.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 0.75rem;
        }
        .logo-icon i {
            font-size: 1.25rem;
            color: white;
        }
        .logo-text {
            font-weight: 700;
            font-size: 1.125rem;
            background: linear-gradient(135deg, #10b981, #14b8a6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        .nav {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        .nav-link {
            color: var(--muted);
            text-decoration: none;
            font-size: 0.875rem;
            font-weight: 500;
            transition: color 0.3s ease;
            padding: 0.5rem 0.75rem;
            border-radius: 0.5rem;
        }
        .nav-link:hover {
            color: var(--foreground);
            background: var(--border);
        }
        .theme-toggle {
            background: transparent;
            border: none;
            color: var(--muted);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            transition: all 0.3s ease;
        }
        .theme-toggle:hover {
            color: var(--foreground);
            background: var(--border);
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }
        .register-section {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0.25rem 0 0.25rem 0;
        }
        .register-card {
            background: rgba(16, 25, 43, 0.98);
            border-radius: 1rem;
            box-shadow: 0 4px 24px 0 rgba(16, 185, 129, 0.08), 0 1px 8px 0 rgba(0,0,0,0.10);
            padding: 2rem 2.5rem 1.5rem 2.5rem;
            max-width: 600px;
            width: 100%;
            color: #fff;
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
            border: 1px solid var(--border);
            animation: fadeInUp 0.7s cubic-bezier(.39,.575,.565,1) both;
        }
        [data-theme="light"] .register-card {
            background: #f8fafc;
            color: #1e293b;
        }
        .register-title {
            font-size: 1.45rem;
            font-weight: 800;
            text-align: center;
            margin-bottom: 0.18rem;
            color: #10b981;
            letter-spacing: -0.5px;
        }
        .register-desc {
            text-align: center;
            color: #cbd5e1;
            font-size: 0.93rem;
            margin-bottom: 1rem;
        }
        [data-theme="light"] .register-desc {
            color: #64748b;
        }
        .register-fields-row {
            display: flex;
            gap: 1.25rem;
        }
        .register-fields-row .form-group {
            flex: 1 1 0;
            margin-bottom: 0;
        }
        .register-fields-row.confirm-row {
            display: flex;
            gap: 1.25rem;
            align-items: flex-end;
        }
        .register-fields-row.confirm-row .form-group {
            flex: 0 0 45%;
        }
        .register-fields-row.confirm-row .register-btn {
            flex: 0 0 55%;
            min-width: 0;
            max-width: none;
            padding: 0;
            font-size: 0.99rem;
            height: 2.4rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        @media (max-width: 800px) {
            .register-card {
                max-width: 400px;
                width: 85%;
                margin: 0 auto;
                padding: 1.5rem 1.25rem;
            }
            .register-fields-row {
                flex-direction: column;
                gap: 0;
            }
            .register-fields-row .form-group {
                margin-bottom: 1rem;
            }
            .register-fields-row.confirm-row .register-btn {
                width: 100%;
                margin-top: 0.5rem;
                height: auto;
            }
        }
        .register-fields-row,
        .form-group {
            margin-bottom: 0.25rem;
        }
        .register-fields-row:last-of-type,
        .form-group:last-of-type {
            margin-bottom: 0;
        }
        .form-label {
            display: block;
            font-weight: 500;
            margin-bottom: 0.25rem;
            font-size: 0.97rem;
        }
        .form-control {
            width: 100%;
            padding: 0.6rem 0.85rem;
            border-radius: 0.6rem;
            border: 1px solid var(--border);
            background: rgba(255,255,255,0.07);
            color: inherit;
            font-size: 0.97rem;
            transition: border-color 0.2s, background 0.2s, box-shadow 0.2s;
            box-sizing: border-box;
        }
        .register-card body[data-theme='dark'] input.form-control,
        .register-card body[data-theme='dark'] input,
        body[data-theme='dark'] .register-card input.form-control,
        body[data-theme='dark'] .register-card input {
            background: rgba(16,25,43,0.18) !important;
            color: #e5e7eb !important;
            backdrop-filter: blur(4px) !important;
            box-shadow: none !important;
            border: 1px solid #334155 !important;
        }
        body[data-theme='dark'] .register-card input.form-control:focus,
        body[data-theme='dark'] .register-card input:focus {
            background: rgba(16,25,43,0.28) !important;
        }
        body[data-theme='dark'] .register-card input.form-control:hover,
        body[data-theme='dark'] .register-card input:hover {
            background: rgba(16,25,43,0.32) !important;
        }
        .form-control:focus {
            outline: none;
            border-color: #10b981;
            background: rgba(255,255,255,0.13);
            box-shadow: 0 0 0 2px rgba(16,185,129,0.08);
        }
        body[data-theme="dark"] .form-control:focus {
            background: rgba(16,25,43,0.28) !important;
        }
        .form-control:hover {
            border-color: #14b8a6;
            background: rgba(20,184,166,0.08);
            box-shadow: 0 1px 4px 0 rgba(16,185,129,0.05);
        }
        body[data-theme="dark"] .form-control:hover {
            background: rgba(16,25,43,0.32) !important;
        }
        .form-control::placeholder {
            color: #64748b;
            font-size: 0.89em;
            opacity: 1;
        }
        .form-text {
            font-size: 0.8rem;
            color: #94a3b8;
            margin-top: 0.18rem;
        }
        [data-theme="light"] .form-text {
            color: #64748b;
        }
        .alert {
            padding: 0.75rem 1rem;
            border-radius: 0.75rem;
            background: #fee2e2;
            color: #b91c1c;
            font-size: 0.98rem;
            margin-bottom: 1rem;
            border: 1px solid #fecaca;
            text-align: center;
        }
        .register-btn {
            width: 100%;
            padding: 0.6rem 0;
            font-weight: 600;
            font-size: 0.99rem;
            border-radius: 0.6rem;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            border: none;
            cursor: pointer;
            background: #10b981;
            color: #fff;
            box-shadow: 0 1px 4px rgba(0,0,0,0.06);
            transition: background 0.2s;
            margin-top: 0.2rem;
            height: 2.6rem;
        }
        .register-btn:hover {
            background: #059669;
        }
        .register-bottom {
            margin-top: 1.2rem;
            text-align: center;
            font-size: 0.93rem;
            color: #94a3b8;
        }
        .register-bottom a {
            color: #3b82f6;
            text-decoration: none;
            font-weight: 500;
            margin-left: 0.25rem;
            transition: color 0.2s;
        }
        .register-bottom a:hover {
            color: #2563eb;
            text-decoration: underline;
        }
        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(40px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* Mobile Responsive Header */
        @media (max-width: 768px) {
            .desktop-nav {
                display: none;
            }

            .mobile-menu-toggle {
                display: flex;
                align-items: center;
                justify-content: center;
                background: rgba(16, 185, 129, 0.1);
                backdrop-filter: blur(8px);
                border: 1px solid rgba(16, 185, 129, 0.2);
                cursor: pointer;
                padding: 0.5rem 0.75rem;
                border-radius: 2rem;
                transition: all 0.3s ease;
            }

            .mobile-menu-toggle:hover {
                background: rgba(16, 185, 129, 0.15);
                border-color: rgba(16, 185, 129, 0.3);
            }

            [data-theme="dark"] .mobile-menu-toggle {
                background: rgba(16, 185, 129, 0.15);
                border-color: rgba(16, 185, 129, 0.3);
            }

            [data-theme="dark"] .mobile-menu-toggle:hover {
                background: rgba(16, 185, 129, 0.2);
                border-color: rgba(16, 185, 129, 0.4);
            }

            .hamburger-pill {
                display: flex;
                flex-direction: column;
                justify-content: space-between;
                width: 16px;
                height: 12px;
            }

            .hamburger-line {
                width: 100%;
                height: 1.5px;
                background: var(--foreground);
                border-radius: 2px;
                transition: all 0.3s ease;
            }

            .mobile-menu-toggle.active .hamburger-line:nth-child(1) {
                transform: rotate(45deg) translate(4px, 4px);
            }

            .mobile-menu-toggle.active .hamburger-line:nth-child(2) {
                opacity: 0;
            }

            .mobile-menu-toggle.active .hamburger-line:nth-child(3) {
                transform: rotate(-45deg) translate(4px, -4px);
            }

            .header-content {
                padding: 0.75rem 1rem;
                flex-wrap: wrap;
                gap: 0.5rem;
            }

            .logo {
                flex: 1;
                min-width: 0;
            }

            .logo-text {
                font-size: 1rem;
            }
        }

        @media (min-width: 769px) {
            .mobile-menu-toggle {
                display: none;
            }
        }

        /* Mobile Menu Modal Styles */
        .mobile-menu-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(8px);
            z-index: 10000;
            animation: fadeIn 0.3s ease-out;
        }

        .mobile-menu-content {
            position: absolute;
            top: 0;
            right: 0;
            width: 280px;
            height: 100%;
            background: var(--background);
            box-shadow: -4px 0 15px rgba(0, 0, 0, 0.1);
            animation: slideInRight 0.3s ease-out;
        }

        .mobile-menu-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 1.5rem;
            border-bottom: 1px solid var(--border);
        }

        .mobile-menu-header h3 {
            margin: 0;
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--foreground);
        }

        .close-mobile-menu {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--muted);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
        }

        .close-mobile-menu:hover {
            background: var(--border);
            color: var(--foreground);
        }

        .mobile-menu-body {
            padding: 1rem 0;
            height: calc(100% - 70px);
            display: flex;
            flex-direction: column;
        }

        .mobile-nav-links {
            flex: 1;
        }

        .mobile-nav-link {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem 1.5rem;
            color: var(--foreground);
            text-decoration: none;
            font-size: 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
            background: none;
            width: 100%;
            text-align: left;
            cursor: pointer;
        }

        .mobile-nav-link:hover {
            background: var(--border);
        }

        .mobile-nav-link i {
            width: 20px;
            color: var(--muted);
        }

        .mobile-menu-footer {
            padding: 1rem 1.5rem;
            border-top: 1px solid var(--border);
        }

        .mobile-theme-toggle {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            width: 100%;
            padding: 0.75rem 1rem;
            background: var(--border);
            border: none;
            border-radius: 0.5rem;
            color: var(--foreground);
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .mobile-theme-toggle:hover {
            background: var(--muted);
        }

        /* Contact Modal Styles */
        .contact-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(8px);
            z-index: 10000;
            align-items: center;
            justify-content: center;
            animation: fadeIn 0.3s ease-out;
        }

        .contact-modal-content {
            background: var(--background);
            border-radius: 1rem;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            max-width: 600px;
            width: 90%;
            max-height: 90vh;
            overflow-y: auto;
            animation: slideIn 0.3s ease-out;
        }

        .contact-modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 2rem;
            border-bottom: 1px solid var(--border);
        }

        .contact-modal-header h3 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--foreground);
        }

        .close-modal {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--muted);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
        }

        .close-modal:hover {
            background: var(--border);
            color: var(--foreground);
        }

        .contact-modal-body {
            padding: 2rem;
        }

        .contact-options {
            display: grid;
            gap: 1.5rem;
        }

        .contact-option {
            display: flex;
            align-items: flex-start;
            gap: 1rem;
            padding: 1.5rem;
            border: 1px solid var(--border);
            border-radius: 0.75rem;
            transition: all 0.3s ease;
        }

        .contact-option:hover {
            border-color: var(--primary);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.1);
        }

        .contact-icon {
            width: 3rem;
            height: 3rem;
            border-radius: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            color: white;
            flex-shrink: 0;
        }

        .contact-icon.email {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        }

        .contact-icon.whatsapp {
            background: linear-gradient(135deg, #10b981, #059669);
        }

        .contact-icon.form {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
        }

        .contact-info {
            flex: 1;
        }

        .contact-info h4 {
            margin: 0 0 0.5rem 0;
            font-size: 1.125rem;
            font-weight: 600;
            color: var(--foreground);
        }

        .contact-info p {
            margin: 0 0 1rem 0;
            color: var(--muted);
            font-size: 0.875rem;
        }

        .contact-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            font-size: 0.875rem;
        }

        .email-btn {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
            color: white;
        }

        .email-btn:hover {
            background: linear-gradient(135deg, #2563eb, #1e40af);
            transform: translateY(-1px);
        }

        .whatsapp-btn {
            background: linear-gradient(135deg, #10b981, #059669);
            color: white;
        }

        .whatsapp-btn:hover {
            background: linear-gradient(135deg, #059669, #047857);
            transform: translateY(-1px);
        }

        .form-btn {
            background: linear-gradient(135deg, #8b5cf6, #7c3aed);
            color: white;
        }

        .form-btn:hover {
            background: linear-gradient(135deg, #7c3aed, #6d28d9);
            transform: translateY(-1px);
        }

        .contact-form {
            margin-top: 1rem;
        }

        .contact-form .form-group {
            margin-bottom: 1.5rem;
        }

        .contact-form .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--foreground);
        }

        .contact-form .form-group input,
        .contact-form .form-group textarea {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            font-size: 0.875rem;
            background: var(--background);
            color: var(--foreground);
            transition: all 0.3s ease;
        }

        .contact-form .form-group input:focus,
        .contact-form .form-group textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
        }

        .form-actions {
            display: flex;
            gap: 1rem;
            justify-content: flex-end;
        }

        .btn-secondary {
            background: var(--border);
            color: var(--foreground);
        }

        .btn-secondary:hover {
            background: var(--muted);
        }

        .btn-primary {
            background: linear-gradient(135deg, #10b981, #14b8a6);
            color: white;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #059669, #0d9488);
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideIn {
            from { 
                opacity: 0;
                transform: translateY(-20px) scale(0.95);
            }
            to { 
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        @keyframes slideInRight {
            from { 
                transform: translateX(100%);
            }
            to { 
                transform: translateX(0);
            }
        }

        @media (max-width: 480px) {
            .header-content {
                padding: 0.5rem 0.75rem;
            }

            .nav {
                gap: 0.25rem;
            }

            .nav-link {
                font-size: 0.75rem;
                padding: 0.3rem 0.5rem;
            }

            .btn {
                font-size: 0.75rem;
                padding: 0.3rem 0.6rem;
            }

            .logo-text {
                font-size: 0.9rem;
            }
            
            .register-section {
                padding: 2rem 1rem 1rem 1rem;
            }
            
            .register-card {
                max-width: 320px;
                width: 100%;
                padding: 1.25rem 1rem;
                margin: 0 auto;
            }
            
            .register-title {
                font-size: 1.25rem;
                margin-bottom: 0.5rem;
            }
            
            .register-desc {
                font-size: 0.85rem;
                margin-bottom: 1.25rem;
            }
            
            .form-label {
                font-size: 0.9rem;
                margin-bottom: 0.3rem;
            }
            
            .form-control {
                padding: 0.5rem 0.75rem;
                font-size: 0.9rem;
                border-radius: 0.5rem;
            }
            
            .register-btn {
                font-size: 0.9rem;
                padding: 0.5rem 0;
                height: 2.4rem;
            }
            
            .register-bottom {
                font-size: 0.85rem;
                margin-top: 1rem;
            }
            
            .container {
                padding: 0 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-content">
            <div class="logo">
                <a href="/" style="text-decoration: none; color: inherit; display: flex; align-items: center; gap: 0.75rem;">
                    <div class="logo-icon">
                        <!-- Removed WhatsApp icon -->
                    </div>
                    <div class="logo-text"><img class="theme-logo" src="/logo-animated.svg" alt="Novi Logo" style="height:56px;vertical-align:middle;"></div>
                </a>
            </div>
            <nav class="nav desktop-nav">
                <a href="/" class="nav-link">Home</a>
                <button onclick="openContactModal()" class="nav-link" style="background: none; border: none; cursor: pointer; font-family: inherit;">Contact</button>
                <a href="/login" class="nav-link">Business Login</a>
                <a href="/admin/dashboard" class="nav-link">Admin</a>
                <button class="theme-toggle" id="themeToggle">
                    <i class="fas fa-sun"></i>
                    <i class="fas fa-moon"></i>
                </button>
            </nav>

            <!-- Mobile Hamburger Menu -->
            <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">
                <div class="hamburger-pill">
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                </div>
            </button>
        </div>
    </header>
    <section class="register-section">
        <form class="register-card" action="/register" method="POST">
            <div>
                <div class="register-title">Create Your Account</div>
                <div class="register-desc">Create your account to start managing deliveries with Novi.</div>
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert"><%= error %></div>
                <% } %>
            </div>
            <div class="register-fields-row">
                <div class="form-group">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" class="form-control" id="name" name="name" required placeholder="e.g., John Doe">
                </div>
                <div class="form-group">
                    <label for="phoneNumber" class="form-label">Phone Number</label>
                    <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" pattern="[0-9]{10,15}" placeholder="e.g., 08012233344" required>
                </div>
            </div>
            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" class="form-control" id="email" name="email" required placeholder="e.g., user@email.com">
            </div>
            <div class="register-fields-row">
                <div class="form-group">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required minlength="6" placeholder="Choose a password (at least 6 characters)">
                </div>
                <div class="form-group">
                    <label for="confirmPassword" class="form-label">Confirm Password</label>
                    <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required minlength="6">
                    <div class="form-text" id="confirmPasswordError" style="display:none;color:#b91c1c;"></div>
                </div>
            </div>
            <button type="submit" class="register-btn">Continue to Business Setup <i class="fas fa-arrow-right"></i></button>
            <div class="register-bottom">
                Already have an account?
                <a href="/login" style="color:#10b981;">Sign in</a>
            </div>
          </form>
    </section>

    <!-- Mobile Menu Modal -->
    <div id="mobileMenuModal" class="mobile-menu-modal">
        <div class="mobile-menu-content">
            <div class="mobile-menu-header">
                <h3>Menu</h3>
                <button class="close-mobile-menu" onclick="toggleMobileMenu()">&times;</button>
            </div>
            <div class="mobile-menu-body">
                <div class="mobile-nav-links">
                    <a href="/" class="mobile-nav-link" onclick="toggleMobileMenu()">
                        <i class="fas fa-home"></i>
                        Home
                    </a>
                    <button onclick="openContactModal(); toggleMobileMenu();" class="mobile-nav-link">
                        <i class="fas fa-envelope"></i>
                        Contact
                    </button>
                    <a href="/login" class="mobile-nav-link" onclick="toggleMobileMenu()">
                        <i class="fas fa-sign-in-alt"></i>
                        Business Login
                    </a>
                    <a href="/admin/dashboard" class="mobile-nav-link" onclick="toggleMobileMenu()">
                        <i class="fas fa-user-shield"></i>
                        Admin
                    </a>
                </div>
                <div class="mobile-menu-footer">
                    <button class="mobile-theme-toggle" id="mobileThemeToggle">
                        <i class="fas fa-sun"></i>
                        <span>Toggle Theme</span>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Contact Modal -->
    <div id="contactModal" class="contact-modal">
        <div class="contact-modal-content">
            <div class="contact-modal-header">
                <h3>Get in Touch</h3>
                <button class="close-modal" onclick="closeContactModal()">&times;</button>
            </div>
            <div class="contact-modal-body">
                <div class="contact-options">
                    <!-- Email Contact -->
                    <div class="contact-option">
                        <div class="contact-icon email">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="contact-info">
                            <h4>Email Support</h4>
                            <p>Get detailed responses within 24 hours</p>
                            <a href="mailto:support@novi.com.ng?subject=Novi%20Support%20Request&body=Hi%20Novi%20Team,%0D%0A%0D%0AI'm%20interested%20in%20learning%20more%20about%20your%20WhatsApp%20delivery%20bot.%0D%0A%0D%0ABusiness%20Name:%0D%0AIndustry:%0D%0AExpected%20Monthly%20Orders:%0D%0A%0D%0AQuestions:%0D%0A%0D%0AThank%20you!" class="contact-btn email-btn">
                                <i class="fas fa-envelope"></i>
                                Send Email
                            </a>
                        </div>
                    </div>

                    <!-- WhatsApp Contact -->
                    <div class="contact-option">
                        <div class="contact-icon whatsapp">
                            <i class="fab fa-whatsapp"></i>
                        </div>
                        <div class="contact-info">
                            <h4>WhatsApp Support</h4>
                            <p>Quick responses during business hours (9AM-6PM WAT)</p>
                            <a href="https://wa.me/2349049763647?text=Hi%20Novi%20Team,%20I'm%20interested%20in%20using%20NOVI%20to%20manage%20my%20business%20operations.%20Can%20you%20help%20me%20get%20started?" class="contact-btn whatsapp-btn" target="_blank">
                                <i class="fab fa-whatsapp"></i>
                                Chat on WhatsApp
                            </a>
                        </div>
                    </div>

                    <!-- Contact Form -->
                    <div class="contact-option">
                        <div class="contact-icon form">
                            <i class="fas fa-file-alt"></i>
                        </div>
                        <div class="contact-info">
                            <h4>Contact Form</h4>
                            <p>Fill out a quick form and we'll get back to you</p>
                            <button onclick="showContactForm()" class="contact-btn form-btn">
                                <i class="fas fa-file-alt"></i>
                                Fill Form
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Contact Form (Hidden by default) -->
                <div id="contactForm" class="contact-form" style="display: none;">
                    <form onsubmit="submitContactForm(event)">
                        <div class="form-group">
                            <label for="contactName">Full Name *</label>
                            <input type="text" id="contactName" name="name" required>
                        </div>
                        <div class="form-group">
                            <label for="contactEmail">Email Address *</label>
                            <input type="email" id="contactEmail" name="email" required>
                        </div>
                        <div class="form-group">
                            <label for="contactBusiness">Business Name</label>
                            <input type="text" id="contactBusiness" name="business">
                        </div>
                        <div class="form-group">
                            <label for="contactMessage">Message *</label>
                            <textarea id="contactMessage" name="message" rows="4" required placeholder="Tell us about your business and how we can help..."></textarea>
                        </div>
                        <div class="form-actions">
                            <button type="button" onclick="hideContactForm()" class="btn btn-secondary">Back</button>
                            <button type="submit" class="btn btn-primary">Send Message</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/theme-utils.js"></script>
    <script>
        // Confirm password validation
        const form = document.querySelector('.register-card');
        const password = document.getElementById('password');
        const confirmPassword = document.getElementById('confirmPassword');
        const confirmPasswordError = document.getElementById('confirmPasswordError');
        form.addEventListener('submit', function(e) {
            if (password.value !== confirmPassword.value) {
                confirmPasswordError.textContent = 'Passwords do not match.';
                confirmPasswordError.style.display = 'block';
                confirmPassword.classList.add('is-invalid');
                e.preventDefault();
            } else {
                confirmPasswordError.textContent = '';
                confirmPasswordError.style.display = 'none';
                confirmPassword.classList.remove('is-invalid');
            }
        });
        confirmPassword.addEventListener('input', function() {
            if (password.value === confirmPassword.value) {
                confirmPasswordError.textContent = '';
                confirmPasswordError.style.display = 'none';
                confirmPassword.classList.remove('is-invalid');
            }
        });

        // Contact Modal Functions
        function openContactModal() {
            document.getElementById('contactModal').style.display = 'flex';
            document.body.style.overflow = 'hidden';
        }

        function closeContactModal() {
            document.getElementById('contactModal').style.display = 'none';
            document.body.style.overflow = 'auto';
            hideContactForm();
        }

        function showContactForm() {
            document.querySelector('.contact-options').style.display = 'none';
            document.getElementById('contactForm').style.display = 'block';
        }

        function hideContactForm() {
            document.querySelector('.contact-options').style.display = 'grid';
            document.getElementById('contactForm').style.display = 'none';
        }

        function submitContactForm(event) {
            event.preventDefault();
            
            const formData = new FormData(event.target);
            const data = {
                name: formData.get('name'),
                email: formData.get('email'),
                business: formData.get('business'),
                message: formData.get('message')
            };

            // Create mailto link with form data
            const subject = 'Novi Contact Form Submission';
            const body = `Name: ${data.name}%0D%0AEmail: ${data.email}%0D%0ABusiness: ${data.business}%0D%0A%0D%0AMessage:%0D%0A${data.message}`;
            
            window.location.href = `mailto:support@novi.com.ng?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
            
            // Show success message
            alert('Thank you! Your message has been prepared. Please send the email that opens.');
            closeContactModal();
        }

        // Close modal when clicking outside
        document.getElementById('contactModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeContactModal();
            }
        });

        // Mobile Menu Functions
        function toggleMobileMenu() {
            const modal = document.getElementById('mobileMenuModal');
            const hamburger = document.querySelector('.mobile-menu-toggle');
            const isOpen = modal.style.display === 'flex';
            
            if (isOpen) {
                modal.style.display = 'none';
                hamburger.classList.remove('active');
                document.body.style.overflow = 'auto';
            } else {
                modal.style.display = 'flex';
                hamburger.classList.add('active');
                document.body.style.overflow = 'hidden';
            }
        }

        // Close mobile menu when clicking outside
        document.getElementById('mobileMenuModal').addEventListener('click', function(e) {
            if (e.target === this) {
                toggleMobileMenu();
            }
        });

        // Sync theme toggle between desktop and mobile
        document.getElementById('mobileThemeToggle').addEventListener('click', function() {
            document.getElementById('themeToggle').click();
        });
    </script>
</body>
</html> 